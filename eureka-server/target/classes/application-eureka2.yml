spring:
  application:
    name: eureka-server
server:
  port: 7002
eureka:
  instance:
    hostname: eureka2
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka-server7001:7001/eureka/
