<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kamfu.mapper.DeliverMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.kamfu.entity.Deliver">
		<id column="ID" property="id" />
		<result column="CabinetName" property="cabinetName" />
		<result column="OpenID" property="openID" />
		<result column="Addr" property="addr" />
		<result column="PostNum" property="postNum" />
		<result column="BoxNum" property="boxNum" />
		<result column="BoxSize" property="boxSize" />
		<result column="DeliverPayMoney" property="deliverPayMoney" />
		<result column="DeliverDate" property="deliverDate" />
		<result column="TakeCode" property="takeCode" />
		<result column="SmsCnt" property="smsCnt" />
		<result column="PackgeStatu" property="packgeStatu" />
		<result column="TakeDate" property="takeDate" />
		<result column="TakePayMoney" property="takePayMoney" />
		<result column="beneficiary" property="beneficiary" />
		<result column="PickupPhone" property="pickupPhone" />
		<result column="MsgOutId" property="msgOutId" />
	</resultMap>

	<!-- 通用查询结果列 -->
	<sql id="BaseColumn">
		*
	</sql>

	<!-- <select id="layuiTree" resultType="cn.stylefeng.guns.base.pojo.node.LayuiTreeNode"> -->
	<!-- select dept_id AS id, pid as "pid", simple_name as title, -->
	<!-- ( -->
	<!-- CASE -->
	<!-- WHEN (pid = 0 OR pid IS NULL) THEN -->
	<!-- 'true' -->
	<!-- ELSE -->
	<!-- 'false' -->
	<!-- END -->
	<!-- ) as "spread" from sys_dept -->
	<!-- where 1=1 -->
	<!-- <if test="deptId != null and deptId != ''"> -->
	<!-- and (pids LIKE CONCAT('%$[',#{deptId},'$]%') escape '$' or dept_id=#{deptId}) -->
	<!-- </if> -->

	<!-- </select> -->

	<!-- <select id="tree" resultType="cn.stylefeng.guns.base.pojo.node.ZTreeNode"> -->
	<!-- select dept_id AS id, pid as "pId", simple_name as name, -->
	<!-- ( -->
	<!-- CASE -->
	<!-- WHEN (pid = 0 OR pid IS NULL) THEN -->
	<!-- 'true' -->
	<!-- ELSE -->
	<!-- 'false' -->
	<!-- END -->
	<!-- ) as "open" from sys_dept -->
	<!-- where 1=1 -->
	<!-- <if test="deptId != null and deptId != ''"> -->
	<!-- and (pids LIKE CONCAT('%$[',#{deptId},'$]%') escape '$' or dept_id=#{deptId}) -->
	<!-- </if> -->
	<!-- </select> -->

	<!-- <select id="list" resultType="map"> -->
	<!-- select -->
	<!-- <include refid="Base_Column_List"/> -->
	<!-- from sys_dept where 1 = 1 -->
	<!-- <if test="condition != null and condition != ''"> -->
	<!-- and simple_name like CONCAT('%',#{condition},'%') or full_name like 
		CONCAT('%',#{condition},'%') -->
	<!-- </if> -->
	<!-- <if test="deptId != null and deptId != ''"> -->
	<!-- and (dept_id = #{deptId} or dept_id in ( select dept_id from sys_dept 
		where pids like CONCAT('%$[', #{deptId}, '$]%') escape '$' )) -->
	<!-- </if> -->
	<!-- order by sort ASC -->
	<!-- </select> -->

	<select id="selectPagedList" resultMap="BaseResultMap"
		parameterType="com.kamfu.model.param.DeliverParam">
		select
		<include refid="BaseColumn" />
		from Deliver where 1=1
		limit #{param.start},#{param.limit}
	</select>


	<select id="selectCount" resultType="java.lang.Integer"
		parameterType="com.kamfu.model.param.DeliverParam">
		select
		count(0)
		from Deliver where 1=1
	</select>
</mapper>
